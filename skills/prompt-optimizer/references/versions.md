# prompt-optimizer version history

Provenance log. Split out of `SKILL.md` 2026-07-24 (Opus 5 migration, progressive
disclosure). Entries are historical records, reproduced verbatim: they describe what a
past pass did and are NOT current instructions. Do not edit a past entry to match
current behavior.

## Versions

- 2026-08-29 — Added the lint-enforced `## Self-improvement` contract block to SKILL.md (capture via the issue tracker); no behavioral change.
- 2026-08-22 (v2026-08-22) — **unlazy completion-discipline merge** . Merged the transferable core of `unlazy` (github.com/Leonxlnx/unlazy, MIT, fetched this session) under a governing constraint the user stated explicitly: the emitted block is ALREADY too long, so the merge had to add near-zero visible output and run in the background. That killed the first carrier design (a new final `<instructions>` step plus two clauses, +3 visible lines per fire). **What landed:** a new `### Completion Contract` section that is a CONTENT gate, evaluated only AFTER the block has already fired — it never fires the skill, never suppresses it, and cannot resurrect a skip-listed prompt. It fires on a two-conjunct test (enumerable set OR derived count, AND silent under-coverage possible) with three exclusions (whole set visible in one short answer; a length cap or a number the user handed you; a specialist owns coverage via handoff). A disjunction was tried first and rejected: it catches every prompt containing a number. When it fires: the final `<instructions>` step IS the countable done-condition, REPLACING the generic verify step rather than adding one (0 lines typical); report-audit and deviation-disclosure are purely behavioural with their evidence riding the closeout sentence CLAUDE.md already requires (0 lines); `<critical>` promotion is a permission, never a mandate. **Output went net-NEGATIVE:** the duplicate `/goal` CTA rendered under the block was removed (the `<suggested_goal>` element carries the pasteable line once), saving 2–4 lines per Document turn — user-authorised, reversing the 2026-08-08 layout-B decision. **Also fixed, user-authorised, in the same change set:** the fire-when-uncertain tiebreaker enumerated the SACRED hard skips but OMITTED the background-task event, which declares itself SACRED 20 lines above — the file contradicted itself and under ambiguity the enumeration would have won. This DOES move Skip text, so the THREE-WAY same-change-set contract fired and was DISCHARGED rather than assumed: both hooks already implement that skip (nudge a0 marker suppression; Stop-hook background-event exclusion in prompt selection), so the edit aligns prose to shipped behaviour and NEITHER HOOK CHANGED — re-proved by pinned hook shasums (`44d25e69…`, `8a373456…`) plus hook-test staying green on its 37 pre-existing assertions. **Rejected from the import:** `GATES.md` artifacts and `gate-check.mjs` (a second unowned enforcement path with no harness is the 2026-08-01 shape); unlazy's Stop hook (a third Stop hook would contend with the two existing ones — issue 55754 is a loop between exactly these); the Depth Tree as a tree (SKILL.md's own design-boundary note rejects planning machinery for a one-shot rewriter; `superpowers:writing-plans` owns it — only the natural-joints heuristic survived, as one Refine bullet); solo/orchestrated mode and the 30-minute subagent threshold (CLAUDE.md owns delegation and is stricter); fresh-context-per-leaf; the `ABANDON: G<n>` syntax (kept the idea, dropped an ID syntax with no ID space); "ignore resource anxiety" (a directive about internal state — kept only its observable consequence). **unlazy provenance, both halves.** Its six-run test reported that baselines already ship zero placeholders (so v1's no-TODO rules are obsolete against current models); that depth arithmetic is fiction (tree 6 cost 1.0–1.5x tree 3, never 8x); and that every SKILL run's report carried 1–3 wrong numbers while BASELINES CARRIED NONE. Read straight, that last finding argues the skill INDUCED the numeric defect through long ledger-keeping runs rather than curing a pre-existing one — so the report-audit rule here is justified from this surface's own record (TITRE batch-3's six silently-unapplied Table-1 overrides; the drain that emailed itself a success; the Gmail probe that returned a confident negative from a probe that never ran), and unlazy's numbers were kept OUT of SKILL.md. Those figures are single-source, n=6, and **UNVERIFIED beyond one fetch** — labelled as such in `references/completion-discipline.md`. **Proof:** hook-test **37/0 pre → 51/0 post** (14 new assertions), and the same 14 run against the frozen pre-merge copy score **43/8 — 8 loud failures**, so the gate cannot pass on the unfixed copy; `lint_agents.py --skill` 0 errors / 0 warnings on both the frozen control and the live copy; region hashes prove the invariants (description `2cfab1e8…`, `### Trigger` `fdae9db5…`, Phase 0 `7f1325a5…` all byte-identical frozen-vs-live; `### Skip` moved by exactly 2 diff lines, the one authorised edit). **UNCHANGED:** the frontmatter description (1021/1024 chars — 3 to spare, so it is mechanically locked anyway); the >1-sentence trigger threshold; skip precedence and the entire Skip list except the one SACRED-list line; the fire-when-uncertain tiebreaker's semantics; the closed schema (no element added, renamed, or dropped — `<suggested_goal>` stays, only its duplicate CTA *rendering* went); the channel x score question matrix; the memory-inference penalty; the bounded multi-dimension refine loop; the no-double-intake handoff protocol and selector; user-override immediacy; and both hook files byte-for-byte. **Follow-up:** the completion rules that live in behaviour rather than in emitted XML have no hook beneath them — the deterministic gate can assert only that the text exists. That trade was directed by the minimal-output constraint; behaviour is covered by a live fault-injection case (a sweep where full coverage is impossible must close with N−1/N and name the miss), not by the harness. **Independent regression (skill-eval, 13 cases = the 9 built-in plus 4 new sweep/count cases that close the built-in set's blind spot):** gate PASS, runner PASS, independent QA scorer PASS, agree=true, 13/13 — both count-word negative controls correctly SKIP and both contract-boundary cases land correctly. The QA scorer also found a REAL defect in the new text and it was fixed before ship: the exclusion clause read "the count is a length cap or a number the user handed you" while the firing condition read "an explicit N" — but an explicit N is ALWAYS user-supplied, so read literally the exclusion nullified its own trigger, and the 13 cases only separated because the worked example anchors "all 47" as canonical. The clause now states the principle outright: an explicit N fires only when it names what must be COVERED, never when it caps what is PRODUCED. Same self-contradiction shape as the SACRED-list bug this change set also fixed — a rule whose two halves disagree resolves to whichever half the reader reaches first.
- 2026-07-12 (v2026-07-12) — **Conversion-enforcement pass** . Diagnosis: the hook injected on the measured miss clusters but nudge→invocation conversion stayed low (fire rate well under the 90–95% target) and `/goal` had zero adoptions — under-firing was the only failure mode. **Selection lift (broad):** the nudge `additionalContext` was rewritten TIGHTER (net shorter — UserPromptSubmit context re-injects and accumulates every turn, GitHub issue 40216) into a reasoned classification plus a pre-send checkpoint; a **FIRST-RESPONSE GATE** was added to the Output Rule (a self-review-before-finalizing pass measurably improves instruction-following — Re5, arXiv 2507.05598); a new **High-value fire patterns** subsection names the five miss clusters (attachment-led, pasted-plan/persona, meta/skill-engineering, dense clinical consults, short-but-substantive) with a mandatory precedence carve-out (skip categories still win); a **continuation-new-work carve-out** (a continuation adding new substantive work fires; pure micro-edits still skip); a **fire-on-ambiguity** tiebreaker stated as the user's standing preference with an enumerated SACRED hard-skip list; and the **description** was rebalanced (1021 chars, 0 angle brackets, fire cues front-loaded). **Enforcement backstop (precision, not universal):** a NEW **ARMED Stop hook** (`prompt-optimizer-stop-check.sh`, registered `Stop` in settings.json — contract per `code.claude.com/docs/en/hooks`) blocks turn completion ONLY on the two highest-confidence miss clusters (attachment-led `@`, pasted-plan/persona) and **fails OPEN everywhere else**; it is billed as a precision backstop for those clusters, not the 90–95% guarantee (the broad lift is nudge + description + fire patterns + tiebreaker, per the RD-1 reframe). The loop guard is mandatory-first (`stop_hook_active` → allow; omitting it caused a documented runaway — GitHub issue 55754); plan/bypass/subagent/headless paths all fail open, with a `CLAUDE_PO_STOP_DISABLE=1` opt-out for scheduled/headless runs. The same-change-set contract is now **THREE-WAY** (SKILL.md Skip list + nudge hook + Stop hook). **/goal MAXIMAL:** `<suggested_goal>` is now default-on (every Document/Artifact task including single-pass, plus Chat tasks with an autonomy signal or multi-turn shape), with a pre-filled CTA line rendered under the block; still suggest-only (a `/goal` starts a loop; the evaluator is a small/fast, tool-less model that judges only transcript-surfaced state; ≤4000-char condition with a natural-language turn bound; no `--max-turns` flag). **Setup-recommender** large-project gate relaxed from two signals to **one strong signal**. **Boundary note** refreshed with GEPA (arXiv 2507.19457, ICLR 2026 — even the 2026 SOTA scored optimizer still needs numeric rollouts against a metric, which this one-shot human-in-the-loop rewriter deliberately lacks). **UNCHANGED:** the closed 5-element schema; the channel × score question matrix; the memory-inference penalty; the bounded multi-dimension refine loop; the no-double-intake handoff protocol + selector rows; user-override immediacy; the >1-sentence trigger threshold; and the hook pre-filter regexes (payload-only change). Citations: GEPA arXiv 2507.19457, Re5 arXiv 2507.05598, hooks contract `code.claude.com/docs/en/hooks`, GitHub issues 55754 + 40216 — every arXiv ID and URL verified by a tool call, no fabricated identifiers. **Follow-up:** confirm the live fire-rate on demand (telemetry re-pull) before locking the 90–95% claim.
- 2026-07-03 (v2026-07-03) — **Flagship research-grade pass** . Karpathy-style audit + verified external research (Anthropic prompt-eng + Agent-Skills docs; the prompt-optimization literature — DSPy 2310.03714, TextGrad 2406.07496, Bsharat "Principled Instructions" 2312.16171, OPRO/APE/PromptBreeder; Karpathy on context-economy), two adversarial red-team passes, and a `skill-eval` deep gauntlet. **Compression (zero behavior change):** the six repeated Question-Bank tables → one options-only table keyed to the Phase 0.C rubric (77→~18 lines); Rules 1/3/4 → cross-references to their canonical homes; Rule 6 user-override → an explicit (a)/(b)/(c) decision tree. **Enrichment:** a one-line DSPy-signature rationale + an explicit "single-pass, no scored-iteration machinery" design boundary (header note); a compact Bsharat instruction-writing menu in Refine; a `/goal` primer; a matrix note anchoring the score-13+ Document exception; Example 2 shows the pre/post memory-penalty sum; Example 3 shows a lens-filled block; a concrete setup-recommender `Skill` call. **Contract change (ledgered):** added a Skip category — "block-would-exceed-answer" (the trivial-Chat guard, previously stated only in Output Rule) — mirrored verbatim into the `UserPromptSubmit` hook in the same change set. **CAPS/tone:** softened gratuitous emphasis per current-model guidance (Anthropic: dial back CRITICAL/MUST stacking) while keeping firmness on the three correctness gates (skip-precedence, fire-when-uncertain, closed schema) and the firing hook. **UNCHANGED:** >1-sentence trigger, skip-precedence, closed 5-element schema, channel×score matrix, memory-penalty strictness, bounded refine loop, handoff gates, user-override immediacy. Eval: deep-tier PASS/PASS on an expanded adversarial bank + baseline regression; contract-change ledger in the backup dir.
- 2026-06-26 (v2026-06-26) — **STORM best-practices pass** . Studied Stanford OVAL's STORM/Co-STORM and folded its two transferable practices in, additively: (1) **perspective-guided intake** — generalized the existing reviewer-lens question (Example 3) into a named Phase 0.E technique: framing/emphasis questions now offer 2–4 concrete task-relevant perspectives/lenses as options instead of an open "what's the emphasis?", bounded by Economy so it adds no questions; (2) **STORM-shaped → `deep-research` handoff** — new Handoff-selector row + a compact route-when-ALL / don't-route-when-ANY test encoding STORM's good/bad-use judgment (route broad grounded multi-source topic surveys built from scratch; never route the user's own draft/data, single-fact lookups, opinion/taste, specialist-owned tasks, or short answers), with no double-intake (deep-research self-asks 2–3 clarifying questions). Rule 5 + one Gotchas row updated to match. **UNCHANGED:** >1-sentence trigger threshold, skip-precedence, closed 5-element schema, channel × score matrix, memory penalty, the bounded multi-dimension refine loop, and the `UserPromptSubmit` hook (no trigger/skip semantics changed). Eval: ~20 new routing+intake cases + 70-case firing/skip regression via `skill-eval` (medium tier) — see plan Testing/Verification.
- 2026-06-10 — Frontier-model dual-compatibility pass. Added a **Frontier-idiomatic output** note to the Refine section so generated `<role>`/`<instructions>` give-the-reason, lead-with-outcome, avoid redundant enumeration, and never demand reasoning-narration — all model-agnostic, robust on the current and prior frontier Claude models. NO change to the trigger threshold (>1 sentence), skip-precedence, closed 5-element schema, channel×score matrix, handoff selector, or the `UserPromptSubmit` hook. Bumped `lastReviewed`. Eval: firing/skip regression PASSED 9/9 (runner + independent QA).
- 2026-06-07c (v2026-06-07c) — Install-compliance fix for the claude.ai / macOS-app skill validator (description ≤1024 chars and NO XML tags). Rewrote the frontmatter `description` to 1001 chars with no angle-bracket tags (dropped the literal `<optimized_prompt>` reference and the `Source:` line, which carried a stray `>`); all trigger/skip semantics preserved (length-needs-a-task, content-production, cue; skip precedence incl. status checks, deterministic commands, in-prompt continuations). Body, refine loop, and hook unchanged. zip dropped in `~/Downloads/` for desktop-app install.
- 2026-06-07b (v2026-06-07b) — Precision pass from a 70-case adversarial eval (40 real prompts mined from transcripts + 15 sample + 15 edge; 10 runner agents → 5 QA scorers → 1 expert synthesis; expert score 7/10, firing 9/10, precision 6/10; artifacts in `internal <task>/<context>/<constraints>` tags, never drop `<inputs>`/`<instructions>`; (5) **handoff selector** expanded to the CLAUDE.md-mandated gates (biostat-kickoff, stats-plan-reviewer, personalization-gate, clinical-citation-audit/citation-verification); (6) **"capped, not converged"** 4th stop reason — surfaces a genuinely-missing input as an `<inputs>` placeholder + question instead of a thin block; (7) **broadened user overrides** ("just give me", count-overrides-both-ways, just-caps-Q-not-fire); (8) **channel tiebreaker** for the Document/Chat boundary. Plus `<critical>` capped at ≤2 and an Economy-same-pass reinforcement. Deferred (expert): mandatory `<suggested_goal>` obligation. Trigger threshold and the multi-dimension bounded loop unchanged.
- 2026-06-07 (v2026-06-07) — Refine engine + firing + distribution upgrade. **Refine** changed from single-pass / single-weakest-dimension to a **bounded multi-dimension loop**: score all 5 dimensions each pass, fix every dimension < 3 simultaneously, resolve trade-offs toward Alignment, stop on all-3 / convergence / 3 passes; runs on every triggered turn, silent (final block only). Design grounded in multi-dimension-refinement and self-refinement findings (plateau by ~3 passes). **Firing** strengthened to fix under-firing: description rewritten in "Use when…" triggering style with a fire-when-uncertain tiebreaker; skip list narrowed (each category requires "no task / no synthesis"); the `UserPromptSubmit` hook hardened from a hedged nudge to a firm "MUST invoke unless skip-listed." Trigger threshold UNCHANGED at >1 sentence (the 75-word ask was moot — ">1 sentence" already dominates it). **Duplicate resolved:** the stale ≥100-word upstream copy in the Cowork/`anthropic-skills` store is symlinked to this canonical file (matching the existing pattern for citation-verification/fabrication-audit/grant-reviewer/icu-clinical-consult/manuscript-reviewer), so Code + Cowork + `anthropic-skills:` all serve this one file; chat installs the exported `.zip` snapshot. Backups: `internal lastReviewed`, Gotchas/Known-Failure-Modes (failure-framed restatement of existing rules), and this Versions log. NO change to: >1-sentence trigger threshold, 5-element XML output, channel×score question matrix, memory penalty, uncertainty override loop, or any handoff gate.
- Pre-2026-06-04 (from frontmatter Source line): user override vs. upstream — measured register; >1-sentence threshold (updated 2026-05-29); 5-element XML output; Document-channel min-Q loosened at score ≥13; Order added as 5th critique dimension; large-project setup-recommender handoff. Memory penalty kept strict per 2026-05-19 audit (Q9).
